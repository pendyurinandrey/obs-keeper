"""PySide6 interface: menu-bar icon plus a window with live levels and settings.

Only this package imports Qt. It holds no detection logic: it edits ``Config`` and renders
``Status``/levels from the ``Monitor``.
"""
